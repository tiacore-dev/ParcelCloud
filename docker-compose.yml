version: '3'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always
    networks:
      - frontend-network
    mem_limit: 16g  # Попробуйте увеличить до 8ГБ или больше

networks:
  frontend-network:
    driver: bridge
